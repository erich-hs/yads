name: customer_orders
version: "1.0"

columns:
  - name: original_col
    type: string

  - name: generated_col
    type: date
    generated_as:
      column: original_col
      transform: truncate
      transform_args: [50]

partitioned_by:
  - column: original_col
    transform: truncate
    transform_args: [50]
