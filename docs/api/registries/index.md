---
icon: "lucide/folder-archive"
---
# Registries

Registries store and version canonical `YadsSpec` definitions so they can be
shared across teams or environments.

## Available registries

- [`FileSystemRegistry`](filesystem.md#yads.registries.filesystem_registry.FileSystemRegistry): Python `fsspec`-based registry for specs on local disks or common cloud filesystems.
