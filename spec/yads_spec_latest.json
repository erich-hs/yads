{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "YADS Spec",
  "description": "A schema for defining data structures in YADS.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The fully qualified name of the table."
    },
    "version": {
      "type": "string",
      "description": "The version of the spec."
    },
    "description": {
      "type": "string",
      "description": "A description of the table."
    },
    "options": {
      "type": "object",
      "properties": {
        "if_not_exists": {
          "type": "boolean",
          "default": false
        },
        "or_replace": {
          "type": "boolean",
          "default": false
        }
      },
      "description": "Flags that modify the CREATE TABLE statement (e.g., EXTERNAL, IF NOT EXISTS)."
    },
    "storage": {
      "type": "object",
      "description": "Defines the physical storage settings of the table.",
      "properties": {
        "format": {
          "type": "string",
          "description": "Unified table format (e.g., 'iceberg', 'orc', 'csv'). Translates to USING or STORED AS."
        },
        "location": {
          "type": "string"
        },
        "tbl_properties": {
          "type": "object",
          "description": "Key-value pairs for the TBLPROPERTIES clause."
        }
      }
    },
    "partitioned_by": {
      "type": "array",
      "description": "Defines the table's partitioning scheme.",
      "items": {
        "$ref": "#/definitions/transformed_column"
      }
    },
    "table_constraints": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/table_constraint"
      }
    },
    "metadata": {
      "type": "object",
      "properties": {
        "owner": {
          "type": "string"
        },
        "retention_days": {
          "type": "integer"
        },
        "granularity": {
          "type": "string"
        }
      }
    },
    "columns": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/named_column"
      }
    }
  },
  "required": ["name", "version", "columns"],
  "definitions": {
    "column": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "type": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "constraints": {
          "$ref": "#/definitions/constraints"
        },
        "params": {
          "type": "object"
        },
        "element": {
          "$ref": "#/definitions/column"
        },
        "fields": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/named_column"
          }
        },
        "key": {
          "$ref": "#/definitions/column"
        },
        "value": {
          "$ref": "#/definitions/column"
        },
        "metadata": {
            "type": "object"
        },
        "generated_as": {
            "$ref": "#/definitions/generation_clause"
        }
      },
      "required": ["type"]
    },
    "generation_clause": {
        "type": "object",
        "properties": {
            "column": { "type": "string" },
            "transform": { "type": "string" },
            "transform_args": {
                "type": "array"
            }
        },
        "required": ["column", "transform"]
    },
    "transformed_column": {
        "type": "object",
        "properties": {
            "column": { "type": "string" },
            "transform": { "type": "string" }
        },
        "required": ["column"]
    },
    "named_column": {
        "allOf": [
            { "$ref": "#/definitions/column" },
            { "required": ["name"] }
        ]
    },
    "table_constraint": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["primary_key", "foreign_key"]
        },
        "name": {
          "type": "string"
        },
        "columns": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "references": {
          "type": "object",
          "properties": {
            "table": {
              "type": "string"
            },
            "columns": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": ["table"]
        }
      },
      "required": ["type", "columns"]
    },
    "constraints": {
      "type": "object",
      "properties": {
        "not_null": {
          "type": "boolean"
        },
        "primary_key": {
          "type": "boolean"
        },
        "foreign_key": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "references": {
              "type": "object",
              "properties": {
                "table": {
                  "type": "string"
                },
                "columns": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              },
              "required": ["table"]
            }
          },
          "required": ["references"]
        },
        "default": {}
      }
    }
  }
} 